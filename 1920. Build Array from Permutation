class Solution {
    public int[] buildArray(int[] nums) {
        int a= nums.length;
        int arr[]=new int[a];
        int i =0;
        while(i<a){
            arr[i]=nums[nums[i]];
            i++;
        }
        return arr;

        
    }
}
